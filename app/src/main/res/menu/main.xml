<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.fourthyearproject.shane.cycled;" >

    <item android:id="@+id/action_settings"
        android:title="@string/action_settings"
        android:orderInCategory="100"
        app:showAsAction="never" />

    <item
        android:id="@+id/bluetooth_search"
        android:orderInCategory="200"
        android:title="bluetooth_search"
        android:icon="@mipmap/ic_bluetooth_white_24dp"
        app:showAsAction="ifRoom"
        />


    <item
        android:id="@+id/bicycle_icon"
        android:orderInCategory="300"
        android:title="bicycle_icon"
        android:icon="@drawable/bicycle_icon"
        app:showAsAction="ifRoom"
        />

    <item
        android:id="@+id/refresh_icon"
        android:orderInCategory="400"
        android:title="Refresh"
        android:icon="@drawable/refresh_icon"
        app:showAsAction="ifRoom"
        />
</menu>